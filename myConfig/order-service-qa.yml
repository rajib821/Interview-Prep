spring:
  application:
    name: order-service

  datasource:
    url: jdbc:mysql://localhost:3306/kode   # changed from mysql → localhost
    username: boot
    password: rajib@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
